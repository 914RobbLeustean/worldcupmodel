# environments
.venv/
__pycache__/
*.pyc
.mypy_cache/
.pytest_cache/
.ruff_cache/

# data: raw + processed are rebuildable/cached locally, never committed.
# data/manual/ and ledger/ ARE committed — they are hand-entered records.
data/raw/
data/processed/

# secrets — never commit API keys (ODDS_API_KEY lives in the shell env, D007/D033)
.env
*.env

# misc
.DS_Store
